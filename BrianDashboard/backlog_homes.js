// Homes in Backlog — MANUALLY MAINTAINED.
// Add/update values below as you calculate them. Keys must match the division
// keys used elsewhere in the dashboard (see staying_divisions.js for reference:
// 'greensboro', 'atlanta_west', 'raleigh_north', etc.).
//
// Any division not listed here shows "—" in the UI until you fill it in.
const BACKLOG_HOMES = {
  // ── Power Divisions (priority — update first) ──
  // 'greensboro':      0,
  // 'greenville':      0,
  // 'raleigh_north':   0,
  // 'raleigh_south':   0,
  // 'indianapolis':    0,
  // 'delaware':        0,
  // 'atlanta_west':    0,
  // 'kansas_city':     0,

  // ── Other STAYING divisions (add as calculated) ──
};
